{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make All",
            "type": "shell",
            "command": "docker run --rm -v ${PWD}:/host -it -w /host liv2/amiga-gcc-amitools make all",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Make Clean",
            "type": "shell",
            "command": "docker run --rm -v ${PWD}:/host -it -w /host liv2/amiga-gcc-amitools make clean",
            "problemMatcher": [],
            "group": {
                "kind": "build",
            }
        }
    ]
}